import React from 'react';
import Navbar from '../Shared/Navbar';
import Carousel from './Carousel';

const Home = () => {
    return (
        <div>
            <Carousel></Carousel>
        </div>
    );
};

export default Home;